<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css"
        integrity="sha384-JcKb8q3iqJ61gNV9KGb8thSsNjpSL0n8PARn9HuZOnIxN0hoP+VmmDGMN5t9UJ0Z" crossorigin="anonymous">
    <title>Quiz Details</title>
</head>

<body>
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <div>Quiz ID : <strong><%= quizDetails._id %></strong> </div>
                <div>Quiz Date : <strong><%= quizDetails.createdAt %></strong> </div>
                <% 
                var quiz = quizDetails.quiz;
                var score = 0;
                for( let i = 0; i < quiz.length; i++ ) { 
                %>
                <% if (quiz[i].userAnswer.toString() == quiz[i].correctAnswer.toString()) { %>
                <% score += parseInt(quiz[i].marks); %>
                <% } %>
                <% } %>
                <div>Quiz Score : <strong><%= score %></strong> </div>
                <a href="/dashboard" class="btn btn-primary" />Dashboard</a>
                <a href="/logout" class="btn btn-danger" />Logout</a>
            </div>
            <div class="col-md-12">
                <table class="table-responsive table table-striped">
                    <thead>
                        <tr>
                            <th scope="col">Question</th>
                            <th scope="col">Your Answer</th>
                            <th scope="col">Correct Answer</th>
                            <th scope="col">Score</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% 
                            for( let i = 0; i < quizDetails.quiz.length; i++ ) { 
                        %>
                        <tr>
                            <td><%= quizDetails.quiz[i].question %></td>
                            <td><%= (quizDetails.quiz[i].userAnswer !== '') ? (quizDetails.quiz[i].userAnswer) : '--' %>
                            </td>
                            <td><%= quizDetails.quiz[i].correctAnswer %></td>
                            <td><%= quizDetails.quiz[i].marks %></td>
                        </tr>
                        <% } %>
                    </tbody>
                    <tfoot>
                        <tr>
                            <td>Total Score</td>
                            <td></td>
                            <td></td>
                            <td><strong><%= score %></strong></td>
                        </tr>
                    </tfoot>
                </table>
            </div>
        </div>
    </div>
</body>
<script src="https://code.jquery.com/jquery-3.5.1.min.js"
    integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0=" crossorigin="anonymous"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"
    integrity="sha384-B4gt1jrGC7Jh4AgTPSdUtOBvfO8shuf57BaghqFfPlYxofvL8/KUEfYiJOMMV+rV"
    crossorigin="anonymous"></script>
<script src="/js/ajax.js"></script>

</html>
