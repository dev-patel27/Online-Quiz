<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css"
        integrity="sha384-JcKb8q3iqJ61gNV9KGb8thSsNjpSL0n8PARn9HuZOnIxN0hoP+VmmDGMN5t9UJ0Z" crossorigin="anonymous">
    <title>Quiz History</title>
</head>

<body>
    <div class="container">
        <div class="row">
            <div class="col-md-6">
                <img width="100px" height="100px" style="border-radius: 50px;" src="/uploads/<%= userData[0].image %>"
                    alt="/uploads/<%= userData[0].image %>">
            </div>
            <div class="col-md-6">
                <strong><%= userData[0].firstName + ' '+ userData[0].lastName%></strong>
            </div>
            <a href="/dashboard" class="btn btn-primary" />Dashboard</a>
            <a href="/logout" class="btn btn-danger" />Logout</a>
            <table class="table-responsive table table-striped">
                <thead>
                    <tr>
                        <th scope="col">QuizID</th>
                        <th scope="col">Score</th>
                        <th scope="col">Created Date</th>
                        <th scope="col">Details</th>
                    </tr>
                </thead>
                <tbody>
                    <% for( let i = 0; i < quizHistory.length; i++ ) { 
                        var score = 0;%>
                    <tr>
                        <th scope="row"><%= quizHistory[i]._id %> </th>
                        <% for( let j = 0; j < quizHistory[i].quiz.length; j++ ) { %>
                        <% if (quizHistory[i].quiz[j].userAnswer.toString() == quizHistory[i].quiz[j].correctAnswer) { %>
                        <% score += parseInt(quizHistory[i].quiz[j].marks); %>
                        <% } %>
                        <% } %>
                        <td><%= score %></td>
                        <td><%=quizHistory[i].createdAt %></td>
                        <td><a href="/quizDetails/<%= quizHistory[i]._id %>">Quiz Details</a></td>
                    </tr>
                    <% } %>
                </tbody>
            </table>
        </div>
    </div>

</body>
<script src="https://code.jquery.com/jquery-3.5.1.min.js"
    integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0=" crossorigin="anonymous"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"
    integrity="sha384-B4gt1jrGC7Jh4AgTPSdUtOBvfO8shuf57BaghqFfPlYxofvL8/KUEfYiJOMMV+rV"
    crossorigin="anonymous"></script>
<script src="/js/ajax.js"></script>

</html>
